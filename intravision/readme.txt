 ‘айлы и папки:

	t_iv_mvc4 - папка c solution
	|
	+ - t_iv_mvc4 папка c веб проектом mvc4
	| - PhoneBook папка с проектом библиотеки доступа к данным телефонной книги

дл€ запуска приложени€ достаточно открыть solution и запустить проект t_iv_mvc4 в нем

по материалам задани€:
	библиотека PhoneBook использует EF дл€ доступа к бд (pb.mdf).
	pb.mdf находитс€ в папке App_Data проекта t_iv_mvc4 и подключаетс€ посредствам директивы в web.config.
	pb.mdf совместим с SQLServer 2008R2 и выше

минимальные возможности поиска:
	предусмотрен параметр в методе в веб приложени€ дл€ фильтрации по строкам тел. книги
	по этому параметру производитс€ соответствующий вызов в библиотеке PhoneBook (PhoneBookAccess.GetDataFilter)

механизм экспорта в excel:
	предусмотрен метод получени€ сформированного xls файла в библиотеке PhoneBook (PhoneBookAccess.GetXlsData)
	использу€ этот метод mvc приложение сможет предоставл€ть выгрузку файла по http запросу
	дл€ более нагл€дного и временного решени€ выгрузки в excel существует метод получени€ файла формата csv

	